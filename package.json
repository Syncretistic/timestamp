{
  "name": "timestamp_microservice",
  "version": "1.0.0",
  "description": "A chat example to showcase how to use `socket.io` with a static `express` server with `async` for control flow.",
  "main": "server.js",
  "repository": "",
  "author": "saverio salaris",
  "dependencies": {
    "async": "~0.2.8",
    "dotenv": "^2.0.0",
    "express": "~4.8.0",
    "socket.io": "~0.9.14"
  }
}
